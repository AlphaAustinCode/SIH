# Development: leave empty to use Vite's /api proxy.
VITE_API_BASE_URL=
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000

# Production: set VITE_API_BASE_URL to the public HTTPS FastAPI URL before building.
